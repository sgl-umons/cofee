[project]
name = "cofee"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.1",
    "gitpython>=3.1.46",
]

[project.scripts]

cofee = "cofee.main:main"